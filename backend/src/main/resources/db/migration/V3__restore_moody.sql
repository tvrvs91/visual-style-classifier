-- Возвращаем класс `moody` обратно. В V2 он был удалён вместе со street как
-- методологически слабый, но на практике переобученная модель сохранила его
-- как 7-й класс (с сокращённой выборкой 500 фото). Удаление moody привело бы
-- к рассинхрону между выходом модели (7 logits) и таблицей styles.
--
-- street остаётся удалённым.
INSERT INTO styles (name) VALUES ('moody')
ON CONFLICT (name) DO NOTHING;
