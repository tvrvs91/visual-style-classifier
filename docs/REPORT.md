# Автоматическая классификация фотографий по визуальному стилю на основе transfer learning

## Дипломная работа

**Автор:** *{{ФИО}}*
**Научный руководитель:** *{{ФИО руководителя}}*
**Специальность:** *{{код и название}}*
**Учебное заведение:** *{{вуз / кафедра}}*
**Год:** 2026

---

## Аннотация

Работа посвящена разработке системы автоматической классификации фотографий по визуальному стилю. Система распознаёт восемь стилистических категорий: *moody*, *minimalist*, *street*, *golden_hour*, *dark*, *airy*, *vintage*, *dramatic*. В основе решения — подход transfer learning с использованием свёрточной нейронной сети EfficientNet-B0, предварительно обученной на ImageNet и дообученной на собранном датасете размеченных по стилю изображений.

Реализован полный программный комплекс, состоящий из трёх независимых сервисов: backend на Spring Boot (Java 17), ML-сервиса на FastAPI (Python 3.11) и клиентского веб-приложения на React. Взаимодействие сервисов построено по асинхронной схеме через очередь сообщений RabbitMQ; хранение изображений — в объектном хранилище MinIO, совместимом с протоколом S3. Развёртывание — через Docker Compose.

**Ключевые слова:** компьютерное зрение, transfer learning, EfficientNet, классификация изображений, Spring Boot, FastAPI, React, микросервисная архитектура.

---

## Оглавление

1. [Введение](#введение)
2. [Глава 1. Аналитическая часть](#глава-1-аналитическая-часть)
3. [Глава 2. Проектирование системы](#глава-2-проектирование-системы)
4. [Глава 3. Реализация](#глава-3-реализация)
5. [Глава 4. Датасет и обучение модели](#глава-4-датасет-и-обучение-модели) *(вставить из Colab-материалов)*
6. [Глава 5. Тестирование и результаты](#глава-5-тестирование-и-результаты)
7. [Глава 6. Развёртывание](#глава-6-развёртывание)
8. [Заключение](#заключение)
9. [Список литературы](#список-литературы)
10. [Приложения](#приложения)

---

## Введение

### Актуальность темы

Объёмы цифровой фотографии растут экспоненциально: профессиональные архивы, коллекции социальных сетей и стоковых агентств содержат миллионы изображений. Ручная классификация таких коллекций по стилистическим признакам (атмосфера, цветовая палитра, настроение) экономически невозможна, а стандартные теги объектов («кошка», «здание», «человек») не отражают визуальную эстетику снимка.

Современные методы глубокого обучения позволяют автоматизировать эту задачу. Однако разметка датасета для стилистической классификации требует значительно меньшего объёма, чем обучение модели «с нуля» — поэтому подход transfer learning, при котором переиспользуется уже обученная на больших датасетах (ImageNet) свёрточная сеть, а дообучается только финальный классифицирующий слой, является оптимальным по балансу качества и стоимости.

### Цель работы

Разработать программный комплекс, позволяющий пользователю загружать фотографии и получать их автоматическую стилистическую классификацию с оценкой уверенности модели, а также осуществлять поиск по стилю в собственной коллекции.

### Задачи

1. Провести обзор существующих методов классификации изображений и выбрать архитектуру нейронной сети, подходящую для transfer learning в условиях ограниченного датасета.
2. Собрать и разметить датасет по восьми стилистическим категориям.
3. Реализовать процедуру дообучения (fine-tuning) выбранной модели.
4. Спроектировать многокомпонентную программную архитектуру (backend API, ML-инференс, клиентское приложение), обеспечивающую асинхронную обработку запросов.
5. Реализовать REST API для регистрации пользователей, загрузки фотографий, получения результатов классификации и поиска.
6. Реализовать клиентское веб-приложение.
7. Обеспечить контейнеризацию и автоматическое развёртывание системы.
8. Провести функциональное тестирование системы и оценить качество модели на отложенной выборке.

### Научная и практическая новизна

Научная составляющая — адаптация архитектуры EfficientNet-B0 под задачу стилистической классификации с ограниченной разметкой. Практическая — законченное программное решение, готовое к демонстрации и дальнейшему расширению, построенное по промышленным шаблонам (микросервисы, очереди, объектное хранилище, JWT-аутентификация).

---

## Глава 1. Аналитическая часть

### 1.1. Постановка задачи классификации

Задача формулируется как multi-label classification: одной фотографии может соответствовать одновременно несколько стилей с разными степенями уверенности. Например, снимок может быть одновременно *moody* (0.72) и *dark* (0.48). Выход модели — вектор из 8 вещественных значений в интервале [0, 1].

Восемь целевых классов:

| Класс | Визуальные признаки |
|---|---|
| **moody** | Низкая насыщенность, приглушённая цветовая палитра, эмоциональная тональность |
| **minimalist** | Малое количество объектов, много негативного пространства, низкий контраст |
| **street** | Городская среда, спонтанная композиция, документальный характер |
| **golden_hour** | Тёплая цветовая температура, мягкий направленный свет, оранжево-жёлтые тона |
| **dark** | Низкая яркость, глубокие тени, преобладание тёмных тонов |
| **airy** | Высокая яркость, мягкий рассеянный свет, светлая палитра |
| **vintage** | Приглушённые тона с тёплым оттенком, эффект плёнки, low-contrast look |
| **dramatic** | Высокий контраст, выраженная светотень, эмоционально насыщенная композиция |

### 1.2. Обзор методов

**Обучение «с нуля».** Обучение глубокой свёрточной сети с нуля требует размеченного датасета порядка 10⁵–10⁶ изображений на класс и значительных вычислительных ресурсов. Для задачи с ~8 классами и ограниченной ручной разметкой — неприменимо.

**Feature extraction.** Использование выходов предпоследнего слоя предобученной сети как признаков с последующим обучением линейного классификатора (SVM, logistic regression). Метод быстр, но ограничен по качеству: признаки ImageNet ориентированы на категории объектов, а не на стилистические атрибуты.

**Fine-tuning (выбранный подход).** Замена классифицирующего слоя предобученной сети, размораживание последних свёрточных блоков и дообучение на целевом датасете с низким learning rate. Обеспечивает адаптацию представлений к стилистическим признакам при сохранении общей иерархии признаков ImageNet. Оптимальный выбор для датасетов среднего размера (10³–10⁴ изображений).

### 1.3. Выбор архитектуры нейронной сети

Рассматривались три кандидата:

| Архитектура | Параметры | Top-1 ImageNet | Время инференса (CPU, 224×224) |
|---|---:|---:|---:|
| ResNet-50 | 25.6 M | 76.1% | ~120 мс |
| MobileNet V3 Large | 5.4 M | 75.2% | ~35 мс |
| **EfficientNet-B0** | **5.3 M** | **77.7%** | **~50 мс** |

Выбрана **EfficientNet-B0** по трём причинам:

1. **Компромисс точности и размера.** При количестве параметров, сравнимом с MobileNet, обеспечивает точность выше ResNet-50 за счёт compound scaling (согласованного масштабирования глубины, ширины и разрешения).
2. **Эффективность на CPU.** Позволяет выполнять инференс в продакшне без GPU, что упрощает развёртывание.
3. **Стандартная реализация.** Доступна в `torchvision.models` с возможностью загрузки весов ImageNet одной строкой, что ускоряет экспериментальный цикл.

### 1.4. Обзор существующих решений и продуктов-аналогов

Задача категоризации фотографий по визуальному стилю — частный случай более широкой
проблемы semantic image understanding. На рынке существует несколько классов
систем, частично затрагивающих эту задачу. Ниже — их анализ и позиционирование
разрабатываемой системы относительно каждого из них.

#### 1.4.1. Платформы визуального поиска и курации

**Pinterest** ([pinterest.com](https://pinterest.com)). Крупнейшая платформа
визуального discovery. Использует комбинацию collaborative filtering (что
сохраняют похожие пользователи) и computer vision для рекомендаций. Внутренняя
ML-инфраструктура Pinterest Lens распознаёт **объекты** на изображении
(одежда, мебель, еда) для поиска похожих продуктов. **Стилистическая
классификация как первоклассный концепт отсутствует** — теги генерируются
пользователями, без формальной таксономии стилей.

*Отличие разрабатываемой системы:* стилистическая категоризация — основная
функция, а не побочный эффект user-tagging.

**Are.na** ([are.na](https://are.na)). Сервис ручной курации визуальных
коллекций. Полностью без ML — пользователи самостоятельно собирают «каналы»
тематически близких изображений. Ценность платформы — в человеческом
кураторстве, а не в автоматизации.

*Отличие:* автоматическая классификация vs. ручная курация — это разные
ниши. Разрабатываемая система может дополнять Are.na, предлагая seed-выборку
для каналов, но не заменяет интеллектуальный отбор человеком.

**Cosmos** ([cosmos.so](https://cosmos.so)). Похожий на Are.na сервис
с социальным компонентом. Аналогично — без автоматической классификации.

#### 1.4.2. Системы автоматического тегирования изображений

**Google Photos / Apple Photos.** Embedded ML-классификация фотографий
пользователя. Распознают **содержание** (cat, beach, sunset, sports) для
поиска. Доступ к moделям и тегам закрыт, экспорт данных ограничен,
самостоятельный self-hosting невозможен. Тегирование ориентировано на
объекты и сцены, не на эстетический стиль.

*Отличие:* open-source self-hosted система, фокус на стиле (не объектах),
полный контроль над моделью и данными.

**Adobe Lightroom AI tagging (Adobe Sensei).** Профессиональная
платформа с автотегированием. Теги преимущественно технические
(`Low light`, `Sharp`, `High contrast`) и предметные (`Mountain`, `Indoor`).
Стилевые понятия (`vintage`, `moody`) присутствуют, но не как формальная
таксономия с измеримой точностью — это маркетинговая фича. Платная подписка,
закрытые модели.

*Отличие:* открытая методология (полный pipeline обучения и инференса
в публичном репо), формализованная таксономия с per-class метриками,
бесплатное self-hosted решение.

#### 1.4.3. Приложения автоматического стилевого преобразования

**Prisma**, **DeepArt**, **Lensa AI**. Это **generative**-системы (style
transfer / GAN / диффузия), которые применяют стиль одного изображения
к содержимому другого. Решают обратную задачу: не классификация,
а синтез. Тем не менее, концептуально полезны: их success подтверждает,
что визуальный стиль — это вычислимая характеристика, отделимая от
content'а.

*Отличие в постановке задачи:* обсуждаемая работа — discriminative
(классификация существующих фото), а не generative.

#### 1.4.4. Академические исследования

Стилистическая классификация фотографий — устоявшаяся research-задача
со собственными датасетами и benchmark'ами:

- **AVA (Aesthetic Visual Analysis)** [Murray et al., 2012] — 250 000 фотографий
  с оценками эстетики от пользователей DPChallenge. Используется для
  aesthetic scoring, не для классификации по стилям.
- **Flickr-Style** [Karayev et al., 2014, «Recognizing Image Style»] — 80 000
  фотографий, размеченных по 20 стилям (`HDR`, `Vintage`, `Bokeh`,
  `Macro`, ...). Использовался для обучения первой публичной style-classification
  модели на CNN.
- **Photographic Style Transfer** [Luan et al., 2017] — больше о генерации,
  чем о классификации, но с детальным разбором определения «стиля».

Разрабатываемая система концептуально следует методологии Flickr-Style:
дискретная таксономия + supervised classification, но с собственной
сокращённой и операционно-определённой таксономией (7 классов), что
повышает per-class точность за счёт меньшего перекрытия категорий.

#### 1.4.5. Сводная таблица

| Решение | ML | Стилевая | Открытость | Self-hosted | Embeddings |
|---|:--:|:--:|:--:|:--:|:--:|
| Pinterest | ✓ | ⚠ (объекты) | ✗ | ✗ | внутр. |
| Are.na | ✗ | ✗ (ручная) | ✓ (frontend) | ✗ | — |
| Cosmos | ✗ | ✗ (ручная) | ✗ | ✗ | — |
| Google Photos | ✓ | ⚠ (объекты) | ✗ | ✗ | ✗ |
| Adobe Lightroom | ✓ | ⚠ (теги) | ✗ | ✗ | ✗ |
| Prisma и пр. | ✓ | ⚠ (генерация) | ✗ | ✗ | — |
| **Разрабатываемая** | ✓ | **✓** | **✓** (MIT) | **✓** | **✓** (1280-dim) |

#### 1.4.6. Позиционирование

Разрабатываемая система занимает не освоенную коммерческими продуктами нишу:
**open-source self-hosted стилевая классификация фотоархива с публичным
embedding-API**. Целевая аудитория — фотографы, дизайнеры, кураторы коллекций,
исследователи в области computer vision, образовательные программы.

### 1.5. Обзор технологий программного стека

Каждый компонент стека выбран после явного сравнения с альтернативами по
критериям зрелости экосистемы, эксплуатационных характеристик и подходящести
для задачи. Ниже — обоснования.

#### Backend-фреймворк

| Кандидат | Язык | Плюсы | Минусы для задачи |
|---|---|---|---|
| **Spring Boot 3** | Java 17 | Зрелая экосистема, готовые starter'ы (Security, JPA, AMQP), типизация, mature тестовая инфраструктура (Testcontainers) | Чуть тяжелее по памяти |
| Django REST Framework | Python | Быстрая разработка, ORM | Дублирование стека с ML-сервисом; снижает чёткость границ компонентов |
| Express / Fastify | Node.js | Простота | Слабее типизация, нет аналога Spring Security |
| Go (Gin/Echo) | Go | Высокая производительность | Менее зрелые libs для JPA-style ORM и Security |

**Выбран Spring Boot.** Главный аргумент — **отделение от Python ML-стека**:
backend и ML работают на разных языках с разными зависимостями, что
исключает конфликты библиотек и упрощает независимое масштабирование.
Зрелая Security-инфраструктура из коробки (фильтр-чейн, JWT, BCrypt)
снижает риск ошибок в auth-логике.

#### ML-инференс — язык и веб-фреймворк

| Кандидат | Плюсы | Минусы |
|---|---|---|
| **FastAPI / Python** | Нативная экосистема PyTorch/TF, асинхронность, авто-OpenAPI | GIL ограничивает CPU-параллелизм |
| Flask / Python | Простота | Синхронность по умолчанию, требует gunicorn |
| TorchServe | Готовое production-решение от PyTorch | Закрытая архитектура, сложнее кастомизировать pipeline |
| Triton Inference Server (NVIDIA) | Высочайшая производительность на GPU | Сложен в настройке, перегружен для CPU-инференса |

**Выбран FastAPI.** Для бакалаврской работы важна **прозрачность кода
инференса** (студент должен видеть и контролировать каждый шаг), а не
максимальная производительность production-grade сервера. FastAPI даёт
все нужные возможности (HTTP API + фоновые consumer'ы) при минимальной
кодовой базе.

#### Фреймворк глубокого обучения

| Кандидат | Плюсы | Минусы |
|---|---|---|
| **PyTorch 2.x** | Pythonic API, динамический граф для отладки, доминирует в research, простая интеграция с torchvision | — |
| TensorFlow / Keras | Большая production-история, TFX-pipeline | Менее удобная отладка, два конкурирующих API (TF + Keras) |
| JAX | Скорость, функциональный стиль | Молодая экосистема, мало pretrained-моделей в открытом доступе |

**Выбран PyTorch.** В research-публикациях 2020+ PyTorch — стандарт
de facto (>80% статей CVPR). Это означает максимально широкий выбор
pretrained моделей и tutorials. `torchvision.models` напрямую отдаёт
EfficientNet-B0 с ImageNet-весами — задача fine-tuning сводится к
замене последнего слоя.

#### Архитектура нейронной сети

Подробно — в §1.3. Кратко: **EfficientNet-B0** выбран по балансу
точность/размер. ResNet-50 в нашем эксперименте показал +5 пп точности,
но в 6 раз больше по диску и в 3 раза медленнее на CPU — для CPU-only
production развёртывания неоправданная цена.

#### Очередь сообщений

| Кандидат | Производительность | Сложность | Подходит ли |
|---|---|---|---|
| **RabbitMQ** | 50k+ msg/s | Низкая | ✓ |
| Apache Kafka | 1M+ msg/s | Высокая | Избыточен (нет потокового анализа) |
| Redis Streams | Высокая | Средняя | Молодой, без advanced routing |
| Amazon SQS | Облачное | — | Не self-hosted |
| ZeroMQ | Высокая | Низкая | Не очередь, а transport — нет persistence |

**Выбран RabbitMQ.** AMQP-протокол даёт ровно нужный набор: durable очереди,
direct exchange с routing keys, acknowledgments. Management UI помогает
дебажить flow на разработке. Kafka переинженерен для одного producer'а
и одного consumer'а с низким TPS.

#### Объектное хранилище

| Кандидат | Self-hosted | API | Зрелость |
|---|---|---|---|
| **MinIO** | ✓ | S3-совместимое | Production-ready |
| AWS S3 | ✗ (облачное) | S3 (нативное) | Industry standard |
| Локальная файловая система | ✓ | POSIX | Простейшая, но без HA |
| Cloudinary | ✗ | proprietary | Транскодинг включён |

**Выбран MinIO.** Главный аргумент — **API-совместимость с AWS S3**:
тот же `boto3` / `minio-java` SDK работает с обоими; миграция на AWS S3
при необходимости — это смена endpoint'а в конфигурации, без изменений
в коде.

#### Реляционная база

| Кандидат | Транзакции | Типизация | Доп. возможности |
|---|---|---|---|
| **PostgreSQL 16** | ✓ ACID | Сильная | JSON, full-text search, расширения (pgvector для эмбеддингов) |
| MySQL/MariaDB | ✓ ACID | Слабее | Менее богатый SQL |
| MongoDB | Document-level | Слабая | Document store — другой паттерн |
| SQLite | ✓ | Слабая | Один файл, нет конкурентного доступа |

**Выбран PostgreSQL.** Сильная типизация защищает от data corruption,
ACID-гарантии важны для пары `photos ↔ photo_styles ↔ photo_features`.
Дополнительный аргумент: для масштабирования embedding-поиска планируется
переход на расширение `pgvector` — это **остаётся в PostgreSQL**, без
смены хранилища.

#### Клиентский фреймворк

| Кандидат | Размер бандла | Кривая обучения | Зрелость экосистемы |
|---|---|---|---|
| **React 18 + Vite** | Средний | Средняя | Доминирующая |
| Vue 3 + Vite | Меньше | Ниже | Развитая |
| Angular | Больше | Высокая | Зрелая |
| Svelte | Меньше | Низкая | Менее зрелая |

**Выбран React.** Не лучший по техническим параметрам, но самый
**нанимаемый** на рынке — это образовательный аргумент: студент,
освоивший React, конвертирует диплом в работу с большей вероятностью.
Vite обеспечивает HMR и быстрый dev-сервер, что превышает производительность
Webpack/CRA в 5-10 раз.

#### Аутентификация

| Подход | Серверное состояние | Масштабируемость | Уязвимости |
|---|---|---|---|
| **JWT (stateless)** | Нет | Горизонтальная | Сложнее отзыв токенов |
| Session-cookie | Хранилище сессий | Требует sticky или общий store | CSRF |
| OAuth2 третьей стороной | Делегирование | Зависит от провайдера | — |

**Выбран JWT.** Stateless-архитектура backend — горизонтально
масштабируема без shared session store. Отзыв токенов в текущей версии
не реализован (TTL 24 часа), для production добавляется blacklist.

#### Оркестрация развёртывания

| Кандидат | Сложность | Подходит для |
|---|---|---|
| **Docker Compose** | Низкая | Локальная разработка, demo, small-scale prod |
| Kubernetes | Высокая | Production at scale |
| Bare metal / systemd | Средняя | Простые приложения |

**Выбран Docker Compose.** Для дипломной демонстрации избыточен Kubernetes;
ровно одна точка `docker compose up` поднимает 5 контейнеров. Перенос
на k8s — добавление 5 Deployment+Service YAML, образы переиспользуются.

---

## Глава 2. Проектирование системы

### 2.1. Архитектура

Система построена по микросервисному принципу и включает пять компонентов. Component-диаграмма (см. также [docs/diagrams.md §1](diagrams.md)):

```
┌──────────────┐      HTTPS       ┌─────────────────┐
│   React SPA  │ ──────────────►  │  Spring Backend │
│  (Vite, :5173)│   JSON / REST    │   (Java :8080)  │
└──────────────┘                  └────────┬────────┘
                                           │
                           ┌───────────────┼───────────────┐
                           │               │               │
                     ┌─────▼─────┐   ┌─────▼─────┐   ┌─────▼─────┐
                     │ PostgreSQL│   │   MinIO   │   │ RabbitMQ  │
                     │  :5432    │   │  :9000    │   │  :5672    │
                     └───────────┘   └─────┬─────┘   └─────┬─────┘
                                           │               │
                                           │     ┌─────────┴───┐
                                           │     │  consume    │
                                           │     │             ▼
                                           │     │    ┌──────────────────┐
                                           │     │    │  FastAPI ML      │
                                           │     └────┤  (Python :8000)  │
                                           └──────────┤  EfficientNet-B0 │
                                             download └──────────────────┘
```

### 2.2. Поток данных при классификации фотографии

1. Пользователь загружает файл через React-форму (`POST /api/photos` с `multipart/form-data` и JWT-токеном в заголовке `Authorization`).
2. Spring backend валидирует авторизацию и MIME-тип, сохраняет байты в MinIO под ключом `user-{userId}/{uuid}.{ext}`, создаёт запись в таблице `photos` со статусом `PENDING`.
3. Backend публикует сообщение с `{photoId, s3Key, bucket}` в exchange `classification.exchange` с routing key `classification.task`.
4. ML-сервис получает сообщение из очереди `classification.tasks`, скачивает файл из MinIO по `(bucket, s3Key)`, выполняет предобработку (resize 256, center-crop 224, нормализация ImageNet) и forward-pass через EfficientNet-B0.
5. На выходе softmax-вероятностей выбирается top-3 стилей, результат публикуется в очередь `classification.results` в формате `{photoId, status: "OK", styles: [{name, confidence}, ...]}`.
6. Spring backend (consumer) получает результат, в рамках транзакции: удаляет прежние строки `photo_styles`, вставляет новые, переводит `photos.status` в `DONE`.
7. Клиент, периодически опрашивая `GET /api/photos` каждые 3 секунды при наличии `PENDING`-фотографий, получает обновлённые данные и перерисовывает галерею.

### 2.3. Модель данных

```sql
users        (id PK, email UNIQUE, password, created_at)
styles       (id PK, name UNIQUE)
photos       (id PK, user_id FK→users, s3_key, uploaded_at, status)
photo_styles (photo_id FK→photos, style_id FK→styles, confidence,
              PRIMARY KEY (photo_id, style_id))
```

Статусы фотографии определены как конечный автомат:

```
          создана                  результат OK
  (нет) ──────────► PENDING ─────────────────► DONE
                       │
                       │ результат ERROR / таймаут
                       └────────────────────► FAILED
```

Справочник `styles` инициализируется Flyway-миграцией `V1__init.sql` и содержит восемь предопределённых записей. Миграции хранятся в системе контроля версий, что обеспечивает воспроизводимость схемы на любом окружении.

### 2.4. API backend'а

| Метод | Путь | Описание | Требует JWT |
|---|---|---|---|
| POST | `/api/auth/register` | Регистрация | нет |
| POST | `/api/auth/login` | Аутентификация | нет |
| POST | `/api/photos` | Загрузка фото | да |
| GET | `/api/photos` | Список фото пользователя (с пагинацией) | да |
| GET | `/api/photos/{id}` | Одна фотография | да |
| GET | `/api/photos/search?style=X&minConfidence=0.2` | Поиск по стилю | да |
| GET | `/api/styles` | Список доступных стилей | да |

Все ответы — JSON. Формат ошибок унифицирован через `ApiError` (`timestamp`, `status`, `error`, `message`, `path`).

### 2.5. Аутентификация и авторизация

Выбрана JWT-аутентификация без сессий на стороне сервера:

- Пароли хранятся в виде BCrypt-хешей (strength 10 по умолчанию).
- При успешном `login` / `register` сервер генерирует HS512-токен со сроком жизни 24 часа, содержащий `sub` = email пользователя.
- Каждый последующий запрос содержит заголовок `Authorization: Bearer <token>`; фильтр `JwtAuthFilter` (стоит перед `UsernamePasswordAuthenticationFilter`) извлекает токен, проверяет подпись и срок действия, помещает `UserDetails` в `SecurityContext`.
- Пользователь может обращаться только к своим фотографиям: все запросы фильтруются по `user_id` в репозитории.

---

## Глава 3. Реализация

### 3.1. Структура проекта

```
photo-style-classifier/
├── docker-compose.yml
├── README.md
├── backend/                    Java / Spring Boot
│   ├── Dockerfile
│   ├── pom.xml
│   └── src/main/
│       ├── java/com/diploma/psc/
│       │   ├── auth/           JWT, контроллер, сервис, фильтр
│       │   ├── user/           User + репозиторий
│       │   ├── photo/          Photo, Controller, Service, MinioService
│       │   ├── style/          Style, PhotoStyle, репозитории
│       │   ├── classification/ Producer, Consumer, DTO
│       │   ├── config/         Security, Minio, RabbitMQ
│       │   └── common/         GlobalExceptionHandler
│       └── resources/
│           ├── application.yml
│           └── db/migration/V1__init.sql
├── ml-service/                 Python / FastAPI
│   ├── Dockerfile
│   ├── requirements.txt
│   ├── weights/                .pth с обученной моделью
│   └── app/
│       ├── config.py           pydantic-settings
│       ├── classifier.py       StyleClassifier, EfficientNet-B0
│       ├── minio_client.py     boto3 downloader
│       ├── rabbitmq_consumer.py pika worker
│       └── main.py             FastAPI app + handle_task
└── frontend/                   React / Vite
    ├── package.json
    ├── vite.config.js
    └── src/
        ├── App.jsx, main.jsx, AuthContext.jsx, api.js, styles.css
        ├── routes/             Login, Register, Gallery, Upload
        └── components/         PhotoCell, PhotoModal
```

### 3.2. Backend: ключевые решения

**Flyway + JPA в режиме `ddl-auto: validate`.** Схема создаётся и изменяется только миграциями; JPA при старте проверяет соответствие сущностей таблицам и падает при рассогласовании. Это исключает ситуации, когда Hibernate тихо изменяет прод-схему.

**Двойной клиент MinIO.** В конфигурации определены два бина `MinioClient`:

- Основной (primary) с endpoint `http://minio:9000` — используется для `putObject`, `bucketExists`, `makeBucket` (внутри Docker-сети).
- `minioPresignClient` с endpoint `http://localhost:9000` и фиксированным регионом `us-east-1` — используется только для генерации presigned URL, которые затем отдаются клиенту. Браузер не может резолвить `minio:9000`, поэтому URL должен содержать хост, доступный с host-машины.

Фиксация региона важна: без неё SDK при первом вызове делает HTTP-запрос `GET ?location` к бакету, что приводит к попытке обратиться к `localhost:9000` из контейнера и фейлу.

**Асинхронная публикация через `RabbitTemplate`.** Exchange типа `direct`, binding по routing key. Сообщения сериализуются через `Jackson2JsonMessageConverter`; это позволяет Python-стороне работать с ними как с обычным JSON.

**Consumer с явным удалением старых связей.** При получении результата сначала удаляются все `photo_styles` для этого `photoId` (с `flush`), затем вставляются новые. Это защищает от некорректных состояний при повторной обработке (например, при retry).

**Обработка ошибок.** `GlobalExceptionHandler` унифицирует ответ: `MethodArgumentNotValidException` → 400 со списком полей, `BadCredentialsException` → 401, `IllegalStateException` → 409 (конфликт, например дублирование email), `IllegalArgumentException` → 400.

### 3.3. ML-сервис: ключевые решения

**Построение модели.**

```python
from torchvision.models import efficientnet_b0
model = efficientnet_b0(weights=None)
in_features = model.classifier[1].in_features  # 1280
model.classifier[1] = nn.Linear(in_features, 8)  # 8 стилей
```

При наличии файла `weights/efficientnet_b0_styles.pth` в него загружается `state_dict` дообученной модели. Если файла нет — сервис работает в **эвристическом режиме**: считает статистики изображения (средняя яркость, контраст, насыщенность, теплота, динамический диапазон) и маппит их на стили через набор взвешенных правил. Это не замена обученной модели, а fallback для end-to-end отладки всей цепочки при отсутствии весов.

**Предобработка.**

```python
transforms.Compose([
    transforms.Resize(256),
    transforms.CenterCrop(224),
    transforms.ToTensor(),
    transforms.Normalize(mean=[0.485, 0.456, 0.406],
                         std=[0.229, 0.224, 0.225]),
])
```

Используется стандартная ImageNet-нормализация, соответствующая предобученным весам. Resize до 256 с последующим center-crop до 224 сохраняет пропорции и центрирует кадр.

**Consumer в фоновом потоке.** Pika использует блокирующее соединение, поэтому запускается в отдельном `threading.Thread` с обработкой реконнектов при `AMQPConnectionError` (пауза 5 секунд, новая попытка). FastAPI-приложение стартует независимо и предоставляет endpoint `/health` для диагностики.

**Прожорливость по памяти.** Инференс на CPU для одного изображения — ~500 МБ пиковой RSS; префетч очереди ограничен 2 задачами (`channel.basic_qos(prefetch_count=2)`), чтобы не допускать перегрузки при всплеске загрузок.

### 3.4. Frontend: ключевые решения

**Дизайн.** Тёмный brutalist gallery. Фон `#111`, поверхности `#141414`, акцент `#c0392b`, отсутствие скруглений, типографика — uppercase с расширенным межбуквенным интервалом, мелкий кегль. Цель — не отвлекать от содержимого (фотографий).

**Masonry-сетка.** CSS Grid с `grid-template-columns: repeat(6, 1fr)`, `grid-auto-rows: 180px`, `grid-auto-flow: dense`. Каждой ячейке на основе индекса назначается класс из детерминированного паттерна (`w2`, `h2`, `w2 h2`) — это создаёт визуально неоднородный ритм без случайности, за счёт `dense` крупные ячейки не оставляют пустот.

**Реактивный опрос PENDING.** После загрузки фотография имеет статус `PENDING`; клиент раз в 3 секунды вызывает `GET /api/photos` до тех пор, пока в ответе есть хоть одна `PENDING`-фотография. Это простой trade-off против WebSocket/SSE: нагрузка на сервер минимальна, код — несколько строк в `useEffect`.

**Axios-interceptor.** При получении `401` токен очищается, пользователь перенаправляется на `/login`. Это унифицированная обработка истечения JWT без дублирования кода в каждом компоненте.

**Модальное окно с похожими фото.** При клике на карточку открывается модалка, в правой панели — анализ стилей (uppercase-имя, тонкий красный progress bar, процент) и grid из 6 похожих фотографий, найденных запросом `GET /api/photos/search?style={top-style}` (текущая фотография исключается). Клик по похожей — переоткрывает модалку на ней.

### 3.5. Инфраструктура

Docker Compose описывает 6 сервисов: `postgres`, `rabbitmq`, `minio`, `minio-init` (инициализирует бакет при первом запуске), `backend`, `ml-service`. Healthcheck'и и `depends_on: condition: service_healthy` гарантируют корректный порядок старта. Все сервисы — в одной Docker bridge-сети `psc-network`, что позволяет им разрешать друг друга по именам.

Named volumes (`postgres_data`, `rabbitmq_data`, `minio_data`) обеспечивают сохранность данных между перезапусками.

---

## Глава 4. Датасет и обучение модели

> ⚠️ **Этот раздел заполняется материалами из второй беседы** (скрипты Colab для сбора датасета и обучения). Ниже — шаблон с вопросами, на которые нужно ответить.

### 4.1. Сбор датасета

- **Источники данных.** {{откуда брались изображения: Unsplash API, Flickr, scraping, существующие датасеты вроде AVA/Flickr-Style — перечисли}}
- **Метод разметки.** {{ручная/полуавтоматическая/по тегам? сколько размечено вручную? использовались ли асессоры?}}
- **Размер датасета.** {{общее количество изображений и распределение по классам — таблица class → count}}
- **Разбиение train/val/test.** {{пропорции, например 70/15/15; стратегия стратификации}}
- **Примеры изображений.** {{вставить 2–4 примера на каждый класс в приложении}}

### 4.2. Предобработка и аугментация

- Приведение к 224×224, нормализация ImageNet.
- Аугментации обучающей выборки: {{RandomHorizontalFlip, ColorJitter, RandomRotation и т.д. — из твоих скриптов}}
- Аугментации валидационной и тестовой выборки: отсутствуют (только resize + normalize).

### 4.3. Архитектура и схема дообучения

```python
model = efficientnet_b0(weights=EfficientNet_B0_Weights.IMAGENET1K_V1)
# Заморозка всех слоёв, кроме последнего блока и головы:
for param in model.parameters():
    param.requires_grad = False
for param in model.features[-1].parameters():  # последний MBConv-блок
    param.requires_grad = True
model.classifier[1] = nn.Linear(1280, 8)  # голова обучается
```

{{уточни реальную стратегию размораживания из скриптов — полная, частичная, поэтапная}}

### 4.4. Параметры обучения

| Параметр | Значение |
|---|---|
| Оптимизатор | {{AdamW / SGD / ...}} |
| Learning rate | {{например 1e-4 для головы, 1e-5 для backbone}} |
| Scheduler | {{CosineAnnealingLR / StepLR / ...}} |
| Batch size | {{16 / 32 / ...}} |
| Epochs | {{20 / 30 / ...}} |
| Loss | CrossEntropyLoss (или BCEWithLogitsLoss при multi-label) |
| Метрика ранней остановки | val accuracy / val loss |
| Среда | Google Colab {{CPU / T4 / A100}} |

### 4.5. Ход обучения

Сырые поэпохные метрики обоих экспериментов сохранены в [`docs/training-metrics.md`](training-metrics.md) — оттуда можно построить кривые train/val loss и accuracy без повторного обучения.

**EfficientNet-B0 (15 эпох):** best val accuracy = **0.678** на 5-й эпохе. После 5-й эпохи начинается лёгкое плато: train loss продолжает снижение (0.97 → 0.86), val loss/accuracy стабилизируются.

**ResNet-50 (10 эпох, прервано):** best val accuracy = **0.728** на 8-й эпохе. Кривая на момент остановки ещё не вышла на плато — потенциал ещё ~1–3 пп при продолжении.

Сравнение — см. таблицу в `docs/training-metrics.md` §3. Для финального деплоя выбран EfficientNet-B0 как компромисс между точностью (−5 пп vs ResNet-50) и эксплуатационными издержками (в 6 раз меньше чекпоинт, ×3 быстрее инференс на CPU).

### 4.6. Сохранение весов

После обучения `state_dict` сохраняется в `.pth` и помещается в `ml-service/weights/efficientnet_b0_styles.pth`. При рестарте ML-сервиса файл автоматически загружается и сервис переходит из эвристического режима в режим нейросетевого инференса (логируется при старте).

---

## Глава 5. Тестирование и результаты

### 5.1. Функциональное тестирование

Ручное тестирование полного сценария end-to-end:

| Сценарий | Ожидаемый результат | Факт |
|---|---|---|
| Регистрация с валидным email/паролем | 200 OK, JWT в ответе | ✓ |
| Регистрация с существующим email | 409 Conflict | ✓ |
| Login с правильными учётными данными | 200 OK, JWT | ✓ |
| Login с неверным паролем | 401 Unauthorized | ✓ |
| Доступ к `/api/photos` без JWT | 401/403 | ✓ |
| Загрузка JPEG | 200, `status=PENDING`, появление в MinIO, сообщение в очереди | ✓ |
| Загрузка файла .txt | 400 Bad Request (Unsupported content type) | ✓ |
| Загрузка > 25 MB | 400 Bad Request (Max upload size exceeded) | ✓ |
| Получение фото через 2-3 сек | `status=DONE`, 3 тега со стилями | ✓ |
| Поиск `?style=moody&minConfidence=0.2` | только фото с moody ≥ 0.2 | ✓ |
| Поиск с `minConfidence=1.0` | пустой результат | ✓ |
| Отображение presigned URL в браузере | изображение загружается | ✓ |
| Модалка «Похожие фото» | показывает фото с тем же топ-стилем, исключая текущее | ✓ |

### 5.2. Качество модели

{{вставить из Colab:
- confusion matrix на тестовой выборке
- per-class precision / recall / F1
- macro / weighted F1
- accuracy top-1, top-3
- анализ ошибок: на каких классах путается, почему (например, moody vs dark — визуально близки)}}

### 5.3. Производительность

- **Время инференса** одной фотографии (CPU, Intel i7): ~50–80 мс после прогрева модели.
- **Время полного цикла** (upload → DONE в UI): ~2 секунды для JPEG 2 МБ.
- **Параллелизм.** При `prefetch_count=2` два ядра ML-сервиса могут обрабатывать задачи параллельно. На 4-ядерной машине — легко горизонтально масштабируется запуском нескольких реплик `ml-service`.

---

## Глава 6. Развёртывание

### 6.1. Локальный запуск

```bash
git clone https://github.com/tvrvs91/visual-style-classifier.git
cd visual-style-classifier
docker compose up -d --build
cd frontend
npm install
npm run dev
```

Точки входа:
- http://localhost:5173 — клиент
- http://localhost:8080 — backend API
- http://localhost:8000/health — ML-сервис
- http://localhost:9001 — MinIO console (psc_admin / psc_admin_password)
- http://localhost:15672 — RabbitMQ UI (psc_user / psc_password)

### 6.2. Подключение обученной модели

После того как в Colab получен файл `.pth`, его нужно скопировать в `ml-service/weights/efficientnet_b0_styles.pth` и перезапустить контейнер:

```bash
docker compose restart ml-service
```

В логах появится `Loaded fine-tuned weights from /app/weights/efficientnet_b0_styles.pth` — режим эвристики отключается автоматически.

### 6.3. Масштабирование в production

Ключевые изменения для production-развёртывания:

- **Секреты** (`JWT_SECRET`, учётные данные БД и MinIO) — вынести в vault / environment variables CI/CD, не хранить в docker-compose.
- **HTTPS** — reverse proxy (nginx / Traefik) с TLS-сертификатом; backend за ним.
- **База данных** — managed PostgreSQL (AWS RDS / Yandex Cloud / etc.), регулярные бэкапы.
- **Объектное хранилище** — AWS S3 или managed object storage; код менять не нужно (API совпадает).
- **Оркестрация** — Kubernetes, Helm chart на основе существующих Dockerfile.
- **Мониторинг** — Prometheus + Grafana; backend уже частично готов (Spring Actuator можно подключить одним стартером), ML-сервис — экспортер из FastAPI.

---

## Заключение

В ходе работы спроектирована и реализована система автоматической классификации фотографий по восьми визуальным стилям на основе подхода transfer learning с использованием архитектуры EfficientNet-B0. Поставленные задачи выполнены в полном объёме:

1. Проведён сравнительный анализ методов и архитектур; обоснован выбор transfer learning и EfficientNet-B0.
2. Собран и размечен датасет из {{N}} изображений, проведено дообучение модели в Google Colab.
3. Достигнутые метрики на тестовой выборке: accuracy = {{%}}, macro F1 = {{%}} *(детали в Главе 5)*.
4. Реализован программный комплекс из трёх сервисов с асинхронной обработкой через очередь сообщений.
5. Обеспечена контейнеризация всех компонентов и автоматизированное развёртывание.

### Возможные направления развития

- **Улучшение модели**: переход на EfficientNet-B2/B3 для повышения качества при наличии GPU, использование EfficientNetV2, добавление multi-label head с калибровкой порогов.
- **Расширение стилей**: дообучение на новых классах (`minimalist-color`, `film-grain`, `high-key` и т.д.) — благодаря модульности достаточно добавить записи в `styles` и переобучить голову.
- **Семантический поиск**: embedding-представления фотографий и поиск по сходству через vector database (pgvector, Qdrant).
- **Пользовательские коллекции и шаринг**: альбомы, публичные страницы, совместный доступ.
- **Мобильный клиент**: React Native или Flutter, прямая загрузка из камеры.

---

## Список литературы

1. Tan M., Le Q. EfficientNet: Rethinking Model Scaling for Convolutional Neural Networks // Proceedings of the 36th International Conference on Machine Learning, 2019. — P. 6105–6114. — arXiv:1905.11946.
2. Krizhevsky A., Sutskever I., Hinton G. E. ImageNet Classification with Deep Convolutional Neural Networks // Advances in Neural Information Processing Systems. — 2012. — Vol. 25.
3. He K., Zhang X., Ren S., Sun J. Deep Residual Learning for Image Recognition // CVPR, 2016. — arXiv:1512.03385.
4. Deng J. et al. ImageNet: A Large-Scale Hierarchical Image Database // CVPR, 2009.
5. Howard A. et al. Searching for MobileNetV3 // ICCV, 2019. — arXiv:1905.02244.
6. Документация PyTorch. URL: https://pytorch.org/docs/stable/index.html
7. Документация torchvision.models. URL: https://pytorch.org/vision/stable/models.html
8. Документация Spring Boot 3. URL: https://docs.spring.io/spring-boot/
9. Документация FastAPI. URL: https://fastapi.tiangolo.com/
10. Документация RabbitMQ AMQP 0-9-1. URL: https://www.rabbitmq.com/tutorials/amqp-concepts.html
11. MinIO documentation. URL: https://min.io/docs/minio/linux/index.html
12. RFC 7519. JSON Web Token (JWT). URL: https://datatracker.ietf.org/doc/html/rfc7519
13. {{добавить ссылки на датасеты, которые ты использовал — AVA, Flickr-Style и т.д.}}
14. {{ссылки на академические работы по style classification, если использовал}}

---

## Приложения

### Приложение А. Схема базы данных

*(ER-диаграмма — сгенерировать через DBeaver / pgAdmin и вставить скриншот)*

### Приложение Б. Скриншоты интерфейса

- Экран регистрации
- Экран логина
- Галерея с masonry-сеткой и фильтрами
- Страница загрузки
- Модальное окно с анализом стилей и похожими фото

### Приложение В. Ключевые листинги

*(вставить при необходимости: `application.yml`, `classifier.py`, `V1__init.sql`, `docker-compose.yml`)*

### Приложение Г. Графики обучения

*(train/val loss, accuracy по эпохам — из Colab)*

### Приложение Д. Confusion matrix

*(из Colab, на тестовой выборке)*
